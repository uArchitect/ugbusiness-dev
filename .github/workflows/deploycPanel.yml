name: Deploy to cPanel

on:
  push:
    branches:
      - main
  workflow_dispatch:  # Manuel çalıştırma seçeneği

env:
  NODE_VERSION: '18'

jobs:
  deploy:
    name: Deploy to cPanel via FTP
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Node.js (if needed for build)
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Deploy to cPanel via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: 77.245.159.55
          username: ugbusine
          password: ug0102umex2024*
          protocol: ftps
          server-dir: /public_html/
          dangerous-clean-slate: false
          dry-run: false
          log-level: minimal
          state-name: .ftp-deploy-sync-state.json
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/assets/build/**
            **/assets/dist/**
            **/assets/plugins/**
            **/system/**
            **/vendor/**
            **/composer.lock
            **/composer.json
            **/.github/**
            **/uploads/**
            **/.env*
            **/.env.*
            **/*.log
            **/*.md
            **/ugajansassets/assets/**
            **/application/cache/**
            **/application/logs/**
            **/*.zip
            **/*.tar.gz
            **/*.rar
            **/package*.json
            **/yarn.lock
            **/.npm/**
            **/.yarn/**
            **/tests/**
            **/test/**
            **/docs/**
            **/documentation/**
            **/*.test.*
            **/*.spec.*
            **/.DS_Store
            **/Thumbs.db
            **/.idea/**
            **/.vscode/**
            **/SIPARIS_MODULU_DOKUMANTASYON.md
            **/MODUL_API_DURUM_RAPORU.md
            **/ARVENTO_API_FLUTTER_DOCUMENTATION.md
            **/SUBSCRIPTION_API_FLUTTER_DOCUMENTATION.md
            **/*.sql
            **/debug_*.php
            **/eskisiparis/**

      - name: Deployment Summary
        if: success()
        run: |
          echo "✅ Deployment completed successfully!"
          echo "🚀 Files deployed to cPanel"
          echo "📅 Deployment time: $(date)"

      - name: Deployment Failed Notification
        if: failure()
        run: |
          echo "❌ Deployment failed!"
          echo "Please check the logs above for details."
          exit 1
