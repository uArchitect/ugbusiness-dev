name: Publish Website to CPanel
on:
  push:
    branches:
      - main
jobs:
  FTP-Deploy-Action:
    name: FTP-Deploy-Action
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      # Deploy to cPanel - Only changed files
      - name: FTP-Deploy-Action
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: 77.245.159.55
          username: ugbusine
          password: ug0102umex2024*
          protocol: ftps
          server-dir: /public_html/
          dangerous-clean-slate: false
          dry-run: false
          log-level: silent
          state-name: .ftp-deploy-sync-state.json
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/assets/**
            **/system/**
            **/vendor/**
            **/composer.lock
            **/composer.json
            **/.github/**
            **/uploads/**
            **/.env*
            **/*.log
            **/*.md
            **/ugajansassets/assets/**
            **/application/cache/**
            **/application/logs/**
            **/*.zip
            **/*.tar.gz
            **/*.rar
            **/package*.json
            **/yarn.lock
            **/.npm/**
            **/.yarn/**
            **/tests/**
            **/test/**
            **/docs/**
            **/documentation/**
            **/*.test.*
            **/*.spec.*
